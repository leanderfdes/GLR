# SPA fallback for Cloudflare Pages: serve index.html for client-side routes so
# deep links / refreshes (e.g. /my-kpis, /dashboard) resolve. API prefixes are
# intercepted by functions/_middleware.js before this, and real static assets are
# served before the catch-all, so only unmatched app routes hit this rule.
/*    /index.html    200
